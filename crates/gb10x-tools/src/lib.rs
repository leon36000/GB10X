#![forbid(unsafe_op_in_unsafe_fn)]
#![deny(missing_docs)]

//! Shared support for GB10X command-line tools.

/// PLEPack CLI parsing helpers.
pub mod plepack;
/// Host-probe CLI rendering helpers.
pub mod probe;
